{
  "name": "pelaobolao",
  "version": "0.29.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "test": "node --test tests/game.test.js tests/version.test.js tests/session-ui.test.js tests/condor.test.js tests/reveal.test.js",
    "test:integration": "firebase emulators:exec --project demo-pelaobolao --only firestore \"node --test --test-concurrency=1 tests/integration.test.js tests/rules.test.js\"",
    "emulators": "firebase emulators:start --project demo-pelaobolao",
    "deploy": "npm run build && firebase deploy --only firestore:rules,firestore:indexes,hosting --project pelaobolao",
    "test:e2e": "firebase emulators:exec --project demo-pelaobolao --only auth,firestore \"playwright test\"",
    "predev": "node scripts/generate-icons.mjs",
    "prebuild": "node scripts/generate-icons.mjs"
  },
  "dependencies": {
    "firebase": "12.19.0"
  },
  "devDependencies": {
    "vite": "8.3.0",
    "firebase-tools": "15.30.1",
    "@firebase/rules-unit-testing": "5.0.2",
    "@playwright/test": "1.63.0"
  },
  "engines": {
    "node": ">=22.12.0"
  }
}
